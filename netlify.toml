[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Astro's static output works out of the box on Netlify.
# Netlify Forms are detected automatically from the hidden form in ContactForm.astro.
